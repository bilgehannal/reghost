package config

import "github.com/bilgehannal/reghost/pkg/reghost"

// Config is an alias for the main config type for convenience
type Config = reghost.Config

// Record is an alias for the main record type for convenience
type Record = reghost.Record
